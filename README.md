# 🛍️ E-commerce en JavaScript

Aplicación de comercio electrónico desarrollada con **JavaScript puro**, HTML y CSS, orientada a reforzar los fundamentos del lenguaje, la manipulación del DOM y la lógica de una aplicación real sin el uso de frameworks.

El proyecto utiliza **archivos JSON para simular una base de datos**, por lo que requiere ejecutarse mediante un **servidor local** para funcionar correctamente.

![Estado](https://img.shields.io/badge/estado-en%20proceso-yellow)


---

## ⚠️ Requisito importante

Este proyecto consume datos desde archivos **JSON**, simulando una base de datos y realizando el acceso de forma asíncrona.  
Debido a esto, **no puede ejecutarse correctamente abriendo el archivo HTML directamente**.

Es necesario tener **Node.js** instalado y ejecutar el siguiente comando desde la carpeta del proyecto:

npx live-server --port=8000

El uso de live-server permite evitar problemas relacionados con la carga de archivos, módulos y políticas de seguridad del navegador.

##  🚀 Tecnologías utilizadas
